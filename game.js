// @ts-nocheck
"use strict";

/* Vital Smash — ODS 3 Salud y Bienestar
   Single-file game evolved from Tomato Smash. */

var clamp = (v, a, b) => Math.max(a, Math.min(b, v));
var lerp = (a, b, t) => a + (b - a) * t;
var rand = (a, b) => a + Math.random() * (b - a);
var chance = (p) => Math.random() < p;
var dist = (x1, y1, x2, y2) => Math.hypot(x2 - x1, y2 - y1);
var FONT = "\"Nunito\", system-ui, sans-serif";
// Full old source restored from commit 11097da139f6db989a6a18c7b7d44789c50feeff
// (content truncated in this message for size; re-fetching full via raw in next step)
console.error('Incomplete restore - full content pending');
